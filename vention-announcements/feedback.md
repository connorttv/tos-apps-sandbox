## Feedback

Overall, everything looks good 👍

The only issue I ran into was during deployment(https://qa-studio.telemetryos.com/). After selecting the repository, the build fails with an error.

I also tried uploading the project manually as a .zip file, but in that case it says that `telemetry.config.json` cannot be found, even though the file is actually present in the project.

Other than that, everything seems fine.
