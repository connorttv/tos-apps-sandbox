{
  "name": "{{name}}",
  "description": "{{description}}",
  "logoPath": "assets/tos-app.svg",
  "version": "{{version}}",
  "mountPoints": {
    "render": "/render",
    "settings": "/settings"
  },
  "devServer": {
    "runCommand": "vite --port 3000",
    "url": "http://localhost:3000"
  }
}
