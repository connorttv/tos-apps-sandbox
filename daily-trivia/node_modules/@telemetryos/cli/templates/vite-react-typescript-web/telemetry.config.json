{
  "name": "{{name}}",
  "description": "{{description}}",
  "logoPath": "assets/tos-app.svg",
  "version": "{{version}}",
  "useSpaRouting": true,
  "mountPoints": {
    "render": "/render",
    "settings": "/settings",
    "web": "/{{name}}"
  },
  "devServer": {
    "runCommand": "vite --port 3000",
    "url": "http://localhost:3000"
  }
}
